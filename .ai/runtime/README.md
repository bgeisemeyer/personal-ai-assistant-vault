# Runtime

This folder is reserved for runtime files created inside a real user vault.

## Intended Runtime Files
- `User-Profile.md`

## Notes
- Runtime files are created from templates and then maintained as live configuration.
- These files belong to the AI operating layer, not to normal vault content.
- They are intentionally kept under `.ai/` so they stay close to the framework configuration.
