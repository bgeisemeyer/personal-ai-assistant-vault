# Session Exports

This folder is reserved for runtime handover files created by `Session-Export`.

## Purpose
- Store structured handovers between sessions.
- Support continuation across tools and sessions without relying on hidden chat history.

## Notes
- These are runtime files, not framework source files.
- They are not normal topic notes.
- Only Markdown files in this folder count as importable session exports.
